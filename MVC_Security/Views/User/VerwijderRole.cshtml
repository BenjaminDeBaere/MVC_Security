@model Microsoft.AspNet.Identity.EntityFramework.IdentityRole
@{
    ViewBag.Title = "VerwijderRole";
}

<h2>@Model.Name</h2>
@using (Html.BeginForm("VerwijderRoleDoorvoeren", "User", new { id = Model.Id }))
{
    <input type="submit" value="Delete"/>
}

